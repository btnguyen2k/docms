title:
  # (string, or map[language-code]text)
  en: Site metadata file
  vi: Tập tin metadata trang web
summary:
  # (string, or map[language-code]text)
  en: "The site metadata file resides at the root directory and contains information such as website's name, description or contact details."
  vi: "Tập tin metadata trang web nằm ở thư mục gốc và lưu giữ thông tin về trang web như tên, mô tả hoặc địa chỉ liên lạc."
icon: # (string) supporting FontAwesome icons
file:
  # (map[language-code]filename)
  en: index-en.md
  vi: index-vi.md
tags:
  # (map[language-code]Array(string))
  en: [metadata, website]
  vi: [metadata, trang web]
